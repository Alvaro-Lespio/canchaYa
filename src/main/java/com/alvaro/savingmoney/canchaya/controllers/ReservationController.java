package com.alvaro.savingmoney.canchaya.controllers;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/reservation")
public class ReservationController {

    //Create reservations -> ADMIN

    //Make an assessment -> user

    //Pay reservation-> user

    //See reservations -> ADMIN

    //See total amount generated until the date -> ADMIN

    //if the user realize a reservation, send a notification to the admin. -> ADMIN and USER



}
